# Claude Code 신규 패치 — 2026-07-14

## v2.1.209 (released 2026-07-14)
- `claude agents` 백그라운드 세션에서 `/model` 및 기타 다이얼로그가 차단되던 버그 수정 (과도한 가드 롤백)

## v2.1.208 (released 2026-07-14)
- 스크린 리더 모드 신규 추가: `--ax-screen-reader` 플래그, `CLAUDE_AX_SCREEN_READER=1` 환경변수, 또는 `"axScreenReader": true` 설정으로 활성화
- vim 모드 `vimInsertModeRemaps` 설정 추가: `jj` → Escape 등 두 키 입력-모드 시퀀스 매핑 지원
- 다수 메모리 누수 수정: MCP 서버 stderr 최대 64MB 누적·LSP 문서 무한 유지·파일 편집 캐시 무한 증가 등 장기 세션 안정성 대폭 개선
- 세션 트랜스크립트 크기 최대 79배 감소, 파일 기록 체크포인트 디스크 사용량 제한으로 디스크·메모리 효율 개선
- MCP 툴 다수 사용 시 툴 풀 어셈블리 캐싱으로 최대 7배 빠른 툴 처리 성능
- 자동 업데이트 후 컨텍스트 창이 200k로 일시 초기화되어 "100% 컨텍스트 사용" 경고가 잘못 표시되던 버그 수정
- `claude -p` 파이프 시 대용량 응답 잘림 수정, `$(…)`/백틱 포함 명령의 위험 작업에 `--dangerously-skip-permissions` 모드에서도 확인 프롬프트 표시
