# Claude Code 신규 패치 — 2026-06-24

## v2.1.187 (released 2026-06-23)
- `sandbox.credentials` 설정 추가 — 샌드박스 명령이 자격증명 파일 및 비밀 환경변수를 읽지 못하도록 차단 가능
- 조직 관리자의 모델 제한 설정 지원 추가(`--model`, `/model`, `ANTHROPIC_MODEL`) — 제한된 모델 선택 시 "조직 설정에 의해 제한됨" 안내 메시지 표시
- 풀스크린 모드에서 셀렉트 메뉴(권한 프롬프트, `/model`, `/config` 등) 마우스 클릭 지원 추가
- 원격 MCP 도구 호출이 응답 없이 5분간 멈추던 버그 수정 — 이제 즉시 오류로 중단(`CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT`으로 조정 가능)
- 한국어/CJK 텍스트 붙여넣기 시 터미널에서 깨짐 현상(mojibake) 수정
