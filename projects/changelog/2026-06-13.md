# Claude Code 신규 패치 — 2026-06-13

## v2.1.176 (released 2026-06-12)
- 세션 제목 자동 언어 감지: 대화 언어로 제목이 생성되며, `language` 설정으로 언어 고정 가능
- `footerLinksRegexes` 신규 설정 추가 — 하단 배지 링크를 정규식으로 사용자/관리자 설정에서 커스터마이징 가능
- Bedrock 자격증명 캐시 개선: `awsCredentialExport` 자격증명을 고정 1시간 대신 실제 `Expiration` 시각까지 캐시
- `availableModels` 강제 적용 버그 수정: 환경 변수(`ANTHROPIC_DEFAULT_*_MODEL`)를 통한 허용 목록 우회 차단, `/fast` 전환 시 허용 목록 외 모델로 전환 거부, Fable 5 자동 모드에서 Opus 4.8 미사용 조직의 폴백 수정
- 훅 `if` 조건 파일 경로 패턴 수정(Edit/Read/Write), Linux 심볼릭 링크 샌드박스 시작 오류, tmux over SSH 클립보드 복사 실패, Remote Control 모델 전환·연결 해제 알림·계정 전환 오류, `/cd`·워크트리 브랜치 정보 잔존, 백그라운드 세션(Windows 포함) 다수 버그 수정

## v2.1.175 (released 2026-06-12)
- `enforceAvailableModels` 관리 설정 추가: 활성화 시 기본 모델도 `availableModels` 허용 목록에 제한되며, 사용자·프로젝트 설정에서 관리자 목록 확장 불가
