# Claude Code 신규 패치 — 2026-05-28

## v2.1.153 (released 2026-05-28)
- `skipLfs` 옵션 추가: `github`/`git` 마켓플레이스 소스에서 클론·업데이트 시 Git LFS 다운로드를 건너뛸 수 있게 됨
- `/model`이 선택한 모델을 새 세션의 기본값으로 저장하도록 변경(IDE 동작과 일치); `s` 키는 현재 세션에만 모델 전환 적용
- npm 전역 설치 환경에서 자동 업데이트 불가 시 일회성 안내 표시, `/doctor`에서 복구 방법 확인 가능
- 버그 수정: stateful MCP 서버 재연결 루프(v2.1.147 회귀), 커스텀 API 게이트웨이로 잘못된 Anthropic OAuth 토큰 전달, Windows PowerShell 설치 시 실패에도 "완료" 보고하는 문제, `claude update`가 릴리스 채널 무시하고 최신 버전 설치하는 문제 수정
- 백그라운드 세션 다수 수정: `/bg` 전환 시 응답 유지, 임시 파일 민감도 권한 프롬프트 오류, Windows 업데이트 실패 시 롤백·복구 로직 개선, VSCode 종료 시 프로세스 미정리 문제 수정
